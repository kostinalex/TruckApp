
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<p><h3>Dispatch List</h3></p>

<a href="/dispatch/New" class="btn btn-primary btn-new-confirmation">New dispatch item</a>

<table id="dispatch-list" class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>Dispatch Id</th>
            <th>Dispatch Name</th>
            <th>Delete</th>
        </tr>
    </thead>
    <tbody></tbody>
</table>


@section scripts
    {
    <script>
        $(document).ready(function () {
            console.log("Document ready");
            var table = $("#dispatch-list").DataTable({
                ajax: {
                    url: "/api/dispatch",
                    dataSrc: ""
                },
                columns: [
                    {
                        data: "id"
                    },
                    {
                        data: "name"
                    }
                    ,
                    {
                        data: "id",
                        render: function (data) {
                            return "<button class='btn-link js-delete' data-dispatch-id=" + data + ">Delete</button>";
                        }
                    }
                ],
                initComplete: function () {
                    console.log("Table ready");
                },
                rowCallback: function (row, data) {
                    $(row).css("cursor", "pointer");
                    $(row).on("click", function (e) {
                        if (e.target.tagName != 'BUTTON') {
                            window.location = "/dispatch/edit/" + data.id;
                        }
                    });
                }
            });

            $("#dispatch-list").on("click", ".js-delete", function () {
                var button = $(this);
                console.log("Delete button was pressed id="+ button.attr("data-dispatch-id"));
                bootbox.confirm("Are you sure you want to delete the item?", function (result) {
                    if (result) {
                        $.ajax({
                            url: "/api/dispatch/" + button.attr("data-dispatch-id"),
                            method: "DELETE",
                            success: function () {
                                console.log("Item was deleted");
                                //table.row(button.parents("tr")).remove().draw();
                                table.ajax.reload();
                            }
                        });
                    }
                });

            });
        });
    </script>
}


